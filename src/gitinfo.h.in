/*
 * gitinfo.h — CI build metadata
 *
 * This file is generated by CMake from src/gitinfo.h.in during the configure
 * step.  The two variables below default to developer-friendly values and are
 * overridden in CI through CMake cache variables (-D) or environment variables
 * of the same name.
 *
 * DO NOT edit the generated gitinfo.h — modify gitinfo.h.in instead.
 */

#pragma once

#define HELLSPAWN_CI_BUILD_ID "@CI_BUILD_ID@"
#define HELLSPAWN_COMMIT_HASH "@COMMIT_HASH@"
